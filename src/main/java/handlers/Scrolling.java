package handlers;

/* Scrolling
 *
 * Stores the value of scrolling speed,
 * which then can be accessed by all classes implementing Scrolling.
 */

public interface Scrolling {
    void setScrollingSpeed(double scrollingSpeed);
}
